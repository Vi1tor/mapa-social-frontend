# Exemplo de variáveis de ambiente para o frontend (Vite)
# Copie para .env e ajuste conforme ambiente.

# Backend local (desenvolvimento)
VITE_API_BASE=http://localhost:5000/api/v1

# Backend produção (Railway) - substitua pela URL real
# VITE_API_BASE=https://SEU_BACKEND.up.railway.app/api/v1
